Coverage track computed from uniquely mapping reads only.

Provided in three normalisations: raw counts, counts per million mapped reads
(mil) and counts scaled to the smallest library (min). Forward and reverse
strands are written as separate tracks.
