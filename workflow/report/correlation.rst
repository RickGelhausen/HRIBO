Spearman correlation of read counts between all libraries, shown as a
heatmap.

Replicates of the same method and condition are expected to correlate more
strongly with each other than with any other library. Deviations from that
pattern point to sample swaps or to a failed replicate.
