Aggregated quality control report produced by MultiQC.

Combines the FastQC reports of the raw, trimmed, mapped, uniquely mapped and
rRNA-depleted reads together with the featureCounts summaries, so that read
loss can be traced through every stage of the workflow.
