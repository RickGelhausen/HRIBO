channels:
 - conda-forge
 - bioconda
dependencies:
 # Adapter trimming. A version change here can change which reads survive
 # trimming, and so every downstream count; see ChangeLog.
 - cutadapt =5.2
