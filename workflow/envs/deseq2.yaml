channels:
 - conda-forge
 - bioconda
 - nodefaults
dependencies:
 - bioconductor-deseq2 =1.50.2
 - r-optparse =1.8.2
 - r-plyr =1.8.9
 # Held at the newest 3.x rather than 4.0: ggplot2 4.0 is a major release and
 # scripts/analyse_variance.R could not be exercised here, so the bump stops
 # short of a breaking one.
 - r-ggplot2 =3.5.2
